{
  "name": "basic",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "format": "turbo run format",
    "format:check": "turbo run format:check",
    "lint": "turbo run lint",
    "clean": "rm -rf node_modules .turbo bun.lockb && turbo run clean"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "turbo": "^2.4.4",
    "typescript": "5.8.2"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.2.16"
  },
  "packageManager": "bun@1.2.16",
  "workspaces": ["web", "packages/*"],
  "trustedDependencies": ["@biomejs/biome", "esbuild", "sharp", "turbo"]
}
